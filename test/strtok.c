#include "../sshell.h"

/**
 * _strtok - tokenises the user commands into arguments
 * @input: the user input
 * @delim: the specified delimiters
 * Return: pointer to the token, or NULL if failed
 */
char *_strtok(char *input, char *delim)
{

}
